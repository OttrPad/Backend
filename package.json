{
  "name": "ottrpad-backend",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:api": "turbo run dev --filter=api",
    "dev:core": "turbo run dev --filter=core",
    "dev:collaboration": "turbo run dev --filter=collaboration",
    "build:api": "turbo run build --filter=api",
    "build:core": "turbo run build --filter=core",
    "build:collaboration": "turbo run build --filter=collaboration",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/node": "^18.x",
    "dotenv-cli": "^10.0.0",
    "install": "^0.13.0",
    "nodemon": "^3.1.10",
    "prettier": "^3.0.0",
    "ts-node": "^10.9.2",
    "turbo": "latest",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
